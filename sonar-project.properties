# Required metadata
sonar.projectKey=Flyway-Database-Example
sonar.projectName=Java MySQL and Flyway demo based on Maven
sonar.projectVersion=0.1

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Language
sonar.language=java

# Encoding of the source files
sonar.sourceEncoding=UTF-8